# ============================================================
# {{appName}} 的 jpackage 配置
# ============================================================
# 本文件提供配置值，由构建脚本解析后传给 jpackage 命令，
# 用于构建原生安装包/运行镜像。
# 代码生成阶段会替换 {{...}} 占位符。
# ============================================================

# --- 前置步骤 ---
# 执行 jpackage 前，需先将依赖拷贝到 target/libs 目录：
#   mvn dependency:copy-dependencies -DoutputDirectory=target/libs
# 同时确保应用主 jar 已构建完成（mvn package）。

# --- 应用元数据 ---
app.name={{appName}}
app.version={{appVersion}}
app.vendor={{appVendor}}

# --- 主入口 ---
main.class={{mainClass}}
main.jar={{artifactId}}-{{version}}.jar

# --- 输入/输出 ---
input.dir=target/libs
output.dir=target/dist

# --- 图标资源 ---
# 图标路径按平台区分（构建脚本根据当前平台自动选择对应文件）：
#   Windows: src/main/resources/icons/app-icon.ico
#   macOS:   src/main/resources/icons/app-icon.icns （也支持 .png）
#   Linux:   src/main/resources/icons/app-icon.png
icon.path=src/main/resources/icons/app-icon

# --- Java 运行时选项 ---
java.options=--enable-native-access=javafx.graphics

# --- Windows 打包 ---
win.type=exe
win.menu=true
win.shortcut=true
win.per.user.install=true

# --- macOS 打包 ---
mac.type=dmg
mac.sign=false
mac.package-name={{appName}}

# --- Linux 打包 ---
linux.type=deb
linux.menu-group=Utility
linux.shortcut=true
