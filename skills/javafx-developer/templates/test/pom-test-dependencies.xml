<!--
  Test Dependencies Snippet for pom.xml
  Usage: Insert these dependencies into the <dependencies> section of your pom.xml
  Required: TestFX 4.0.18 + JUnit 5 + Monocle (for headless CI testing)
-->
<dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter</artifactId>
    <version>5.10.2</version>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>org.testfx</groupId>
    <artifactId>testfx-core</artifactId>
    <version>4.0.18</version>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>org.testfx</groupId>
    <artifactId>testfx-junit5</artifactId>
    <version>4.0.18</version>
    <scope>test</scope>
</dependency>
<!-- Monocle: enables headless JavaFX testing in CI environments (no display required) -->
<dependency>
    <groupId>org.testfx</groupId>
    <artifactId>openjfx-monocle</artifactId>
    <version>jdk-21+27</version>
    <scope>test</scope>
</dependency>

<!--
  Required Maven Surefire Plugin configuration (add to <build><plugins>):
  Ensures JUnit 5 tests run and headless mode is configured for CI.
-->
<!--
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-surefire-plugin</artifactId>
    <version>3.2.5</version>
    <configuration>
        <argLine>
            --enable-native-access=javafx.graphics
            -Dtestfx.robot=awt
            -Dtestfx.headless=true
            -Dprism.order=sw
            -Dprism.text=t2k
        </argLine>
    </configuration>
</plugin>
-->
